# FlappyBird WASM

A FlappyBird game written in Rust and compiled to WebAssembly using wasm-bindgen.

## Build

```bash
wasm-pack build --target web
```

## Run

Serve the directory with any static server and open index.html in a browser.